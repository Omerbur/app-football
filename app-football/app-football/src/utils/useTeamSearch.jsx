import {useEffect, useState} from 'react'
import { fetchTeams } from './apiService'

export default function useTeamSearch(query, pageNumber) {
 
    useEffect(() => {
        

    }, [query, pageNumber])
 
    return 
}
